#include <iostream>

int main()
{
	std::cout << "\tThis (\") is a quote, and this (\\) is a backslash." << std::endl;
	std::cout << "\t\tThis (\") is a quote, and this (\\) is a backslash." << std::endl;
	std::cout << "\t\t\tThis (\") is a quote, and this (\\) is a backslash." << std::endl;
	return 0;
}	
